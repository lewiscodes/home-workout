import { Styles } from "../../types";

const styles: Styles = {
  button: {
    backgroundColor: 'transparent',
    padding: '2rem',
    border: '3px solid black',
    borderRadius: 4,
    cursor: 'pointer',
    width: 400,
    fontSize: 20
  }
}

export default styles;
